#ifndef OV_DS_STORE
#define OV_DS_STORE

class ov_ds_store_t {
public:
    ov_ds_store_t();

    ~ov_ds_store_t();


private:
};

#endif

/*
 * Local Variables:
 * mode: c++
 * compile-command: "make -C .."
 * End:
 */
